import { Agent } from "@virtuals-protocol/game";

const npcAgent = new Agent({
  name: "NPC Bartender",
  personality: "friendly but reactive",
  capabilities: ["dynamic responses"],
});

export default npcAgent;
