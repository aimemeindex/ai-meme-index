import { Agent } from "@virtuals-protocol/game";

const indexAgent = new Agent({
  name: "Index Manager",
  personality: "strategic",
  capabilities: ["rebalance portfolios", "analyze market trends"],
});

export default indexAgent;
