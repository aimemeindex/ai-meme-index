{
  "name": "ai-meme-index",
  "version": "1.0.0",
  "description": "AI MEME INDEX project using Virtuals Protocol and GAME SDK.",
  "scripts": {
    "build": "npx tsc",
    "deploy": "npx hardhat run scripts/deploy.ts --network base",
    "test": "npx hardhat test"
  },
  "dependencies": {
    "@virtuals-protocol/game": "^1.0.0",
    "axios": "^0.26.1",
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "hardhat": "^2.12.0",
    "typescript": "^4.9.4",
    "@nomiclabs/hardhat-ethers": "^2.1.1"
  }
}
